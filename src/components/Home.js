import React from "react";
import {
  Text,
  View,
} from 'react-native'

export const Home = () => (
    <View style={{
      display: "flex",
    }}>
      <Text>
        Home
      </Text>
    </View>

);